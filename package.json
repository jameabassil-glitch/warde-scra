// package.json
{
  "name": "woo-stock-sync",
  "version": "1.0.0",
  "description": "Sync fabric stock from Warde into WooCommerce",
  "main": "sync.js",
  "scripts": {
    "sync": "node sync.js"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^10.1.0"
  }
}
